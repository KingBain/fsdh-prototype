@namespace Datahub.Portal.Scaffold.Pages.Workspace
@attribute [Authorize]
@page "/create-workspace"
@page "/creer-espace-de-travail"

<PageTitle>Create workspace</PageTitle>

<PlaceholderPanel
    Title="Create workspace placeholder"
    Summary="Workspace creation originally orchestrated approvals, provisioning, and messaging. The scaffold keeps the route alive without attempting to reproduce those workflows."
    TodoNote="Rebuild the creation wizard against real provisioning and persistence services."
    ImagePath="/img/servers.png" />
